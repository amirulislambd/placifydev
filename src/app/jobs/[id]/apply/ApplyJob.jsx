import React from 'react';

const ApplyJob = ({job}) => {

    return (
        <div>
            <h1>Apply for this job</h1>
            <button>Apply</button>
        </div>
    );
};

export default ApplyJob;